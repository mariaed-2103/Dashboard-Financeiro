# Aqui o Spring vai tentar ler a variável do Render; se não achar, usa o localhost (bom para você continuar desenvolvendo)
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/finance_dashboard}

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

master.encryption.key=${MASTER_ENCRYPTION_KEY}